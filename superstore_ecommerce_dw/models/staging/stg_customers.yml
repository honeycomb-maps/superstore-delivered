version: 2

models:
  - name: stg_customers
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
      - name: email
        tests:
          - not_null
      - name: segment
        tests:
          - accepted_values:
              values: ['Consumer', 'Corporate', 'Home Office']